
// root application div
const getDiv = document.querySelector("#root");

// add header, footer and content section
getDiv.appendChild(Header());

getDiv.appendChild(Projects());

getDiv.appendChild(Footer());
